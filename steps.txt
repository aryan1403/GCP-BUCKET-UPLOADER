sudo apt install git -y
sudo apt install -y python3-venv python3-pip
Edit .env file and make gcpstoragekey.json
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
python ordergenerator.py